json.extract! department, :id, :user_id, :name, :manager, :via, :civico, :cap, :citta, :provincia, :latitude, :longitude, :dep_map, :dep_event, :description, :floors, :number_of_spaces, :created_at, :updated_at
json.url department_url(department, format: :json)
